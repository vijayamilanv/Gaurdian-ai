repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.21.2
    hooks:
      - id: gitleaks
        name: Detect secrets (gitleaks)
        description: Prevent committing credentials, API keys, and tokens
        # Runs on every commit — fast, no network needed after first install
        stages: [pre-commit]
